machine:
  ruby:
    version: 2.2.2
  environment:
    RACK_ENV:  test
    RAILS_ENV: test

machine:
  ruby:
    version: 2.2.2

test:
  override:
    - bundle exec rspec --color spec --format progress
